export enum apiUrl {
  LOGIN = '/api/login',
  APPOINTMENT = '/api/appointments',
  CLINICS = '/api/clinics',
};

export enum RequestMethod {
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
  PATCH = 'PATCH',
  DELETE = 'DELETE',
}