export interface IClinic {
  address: string;
  city: string;
  clinicId: string;
  credentials: string;
  name: string;
  phoneNumber: string;
  providerName: string;
  state: string;
  zipCode: string;
  startTimes: Array<string>;
}
